version: 2

models:
  - name: dim_date
    columns:
      - name: full_date
        data_tests:
          - unique
          - not_null

  - name: dim_location
    columns:
      - name: location_id
        data_tests:
          - unique
          - not_null

  - name: dim_time_of_day
    columns:
      - name: hour
        data_tests:
          - unique
          - not_null

  - name: fct_trips
    columns:
      - name: trip_type
        data_tests:
          - not_null
          - accepted_values:
              values: ['yellow', 'green', 'fhvhv', 'fhv']

      - name: pickup_datetime
        data_tests:
          - not_null

      - name: pickup_date
        data_tests:
          - relationships:
              to: ref('dim_date')
              field: full_date

      - name: dropoff_date
        data_tests:
          - relationships:
              to: ref('dim_date')
              field: full_date

      - name: pickup_hour
        data_tests:
          - relationships:
              to: ref('dim_time_of_day')
              field: hour

      - name: dropoff_hour
        data_tests:
          - relationships:
              to: ref('dim_time_of_day')
              field: hour

      - name: pickup_location_id
        data_tests:
          - relationships:
              to: ref('dim_location')
              field: location_id

      - name: dropoff_location_id
        data_tests:
          - relationships:
              to: ref('dim_location')
              field: location_id

  - name: mart_zone_hourly
    columns:
      - name: pickup_date
        data_tests:
          - not_null

      - name: pickup_hour
        data_tests:
          - not_null

      - name: trip_type
        data_tests:
          - not_null
          - accepted_values:
              values: ['yellow', 'green', 'fhvhv', 'fhv']

      - name: pickup_location_id
        data_tests:
          - not_null
name: nyc_tlc_analytics # Change from the default, `my_new_project`

version: 1.0.0

profile: nyc_tlc_analytics # Change from the default profile name, `default`

model-paths: ["models"]

models:
  nyc_tlc_analytics:
      staging: 
        +materialized: view